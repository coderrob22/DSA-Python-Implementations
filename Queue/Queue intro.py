# FIFO
# First in, first out
# Removing = Dequeue
# Adding = Enqueue
# Dequeue from tail = O(1) but enqueue from tail/last is O(n)
# Enqueue or dequeue from head/first is O(1)

# BOTH adding to and removeing from a Queue is O(1).